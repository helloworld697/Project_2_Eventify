-- Script to run the Eventify database schema
-- Run this in MySQL with: mysql -u root -p < schema.sql
-- Or copy and paste the contents into MySQL Workbench

-- To use this file from command line:
-- mysql -u root -p eventify_db < schema.sql

-- Or if database doesn't exist yet:
-- mysql -u root -p < schema.sql

-- Note: Make sure .env has correct credentials before running backend
